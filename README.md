# WeatherApp

WeatherApp uses openWeather api to fetch weather data for different cities and uses redux for state management.

## Installation

After cloning use the package manager [yarn] to install dependencies.

```bash
yarn
```

```bash
yarn dev
```
